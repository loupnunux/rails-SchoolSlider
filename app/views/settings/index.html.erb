<h1>Settings</h1>

<%= form_tag "/settings", method: "patch" do %>
  <h2>General</h2>
  <p>
  Site Name : <input type="string" name="site_name" value="<%= @settings.site_name %>" />
  <br>
  Message d'acceuil : <textarea type="text" class="form-control" rows="3" name="slider_message"><%= @settings.slider_message %></textarea>
  <br>
  </p>

  <h2>SlideShow</h2>
  <p>
  Interval : <input type="integer" name="slider_interval" value="<%= @settings.slider_interval %>" /> seconde(s)
  <br>

  <!-- Welcome -->
  <% if @settings.full_welcome == true %>
    <input type="checkbox" name="full_welcome" value='true' checked='checked' />
  <% else %>
    <input type="checkbox" name="full_welcome" value='true' />
  <% end %> Page de bienvenue en plein ecran
  <br>

  <!-- Event -->
  <% if @settings.slider_event == true %>
    <input type="checkbox" name="slider_event" value='true' checked='checked' />
  <% else %>
    <input type="checkbox" name="slider_event" value='true' />
  <% end %> Afficher les événements
  <br>

  <!-- Birthday -->
  <% if @settings.slider_birthday == true %>
    <input type="checkbox" name="slider_birthday" value='true' checked='checked' />
  <% else %>
    <input type="checkbox" name="slider_birthday" value='true' />
  <% end %> Afficher les anniversaires
  <br>

  <!-- Saint -->
  <% if @settings.slider_saint == true %>
    <input type="checkbox" name="slider_saint" value='true' checked='checked' />
  <% else %>
    <input type="checkbox" name="slider_saint" value='true' />
  <% end %> Afficher les fêtes du jour
  <br>

  <!-- Biblical -->
  <% if @settings.slider_biblical == true %>
    <input type="checkbox" name="slider_biblical" value='true' checked='checked' />
  <% else %>
    <input type="checkbox" name="slider_biblical" value='true' />
  <% end %> Afficher les citations bibliques
  <br>

  <!-- Learn -->
  <% if @settings.slider_learn == true %>
    <input type="checkbox" name="slider_learn" value='true' checked='checked' />
  <% else %>
    <input type="checkbox" name="slider_learn" value='true' />
  <% end %> Afficher les citations célèbres
  <br>

  <!-- Menu -->
  <% if @settings.slider_menu == true %>
    <input type="checkbox" name="slider_menu" value='true' checked='checked' />
  <% else %>
    <input type="checkbox" name="slider_menu" value='true' />
  <% end %> Afficher le menu<br>
  &nbsp;&nbsp;&nbsp;URL du menu : <textarea type="text" class="form-control" rows="1" name="url_menu"><%= @settings.url_menu %></textarea>
  <br>

  <!-- Meteo -->
  <% if @settings.slider_meteo== true %>
    <input type="checkbox" name="slider_meteo" value='true' checked='checked' />
  <% else %>
    <input type="checkbox" name="slider_meteo" value='true' />
  <% end %> Afficher la météo<br>
  &nbsp;&nbsp;&nbsp;Code à recupérer sur <a href="https://www.tameteo.com/widget/">TaMeteo</a>, faire l'export en image, et récupérer uniquement l'url du 'png' : <textarea type="text" class="form-control" rows="1" name="meteo_code"><%= @settings.meteo_code %></textarea>
  <br>
  
  </p>

  <br>
  <input type="submit" value="Mettre à jour" />
<% end %>

