<%= form_with(model: learn, local: true) do |form| %>
  <% if learn.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(learn.errors.count, "error") %> prohibited this learn from being saved:</h2>

      <ul>
      <% learn.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :quote %>
    <%= form.text_area :quote, id: :learn_quote %>
  </div>

  <div class="field">
    <%= form.label :author_id %>
    <%= form.collection_select :author_id, Author.order(:name), :id, :name, include_blank: true  %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
