<%= form_with(model: learn, local: true) do |form| %>
  <% if learn.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(learn.errors.count, "error") %> prohibited this learn from being saved:</h2>

      <ul>
      <% learn.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :picture %>
    <%= form.file_field :picture %>
  </div>

  <div class="field">
    <%= form.label :citation %>
    <%= form.text_area :citation, id: :learn_citation %>
  </div>

  <div class="field">
    <%= form.label :source %>
    <%= form.text_area :source, id: :learn_source %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
