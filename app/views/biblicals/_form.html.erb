<%= form_with(model: biblical, local: true) do |form| %>
  <% if biblical.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(biblical.errors.count, "error") %> prohibited this biblical from being saved:</h2>

      <ul>
      <% biblical.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :quote %>
    <%= form.text_area :quote, id: :biblical_quote %>
  </div>

  <div class="field">
    <%= form.label :source %>
    <%= form.text_field :source, id: :biblical_source %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
